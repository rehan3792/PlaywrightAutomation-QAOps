import {test, expect} from '@playwright/test';

test("Handle Upload Single File", async ({page}) => {
    page.goto('');
});

test.skip("Handle Upload Multiple File", async ({page}) => {
    page.goto('');
});